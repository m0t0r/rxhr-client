export enum RequestMethod {
  GET,
  POST,
  PUT,
  DELETE
}

export enum ResponseContentType {
  Text,
  Json,
  ArrayBuffer,
  Blob
}

export enum ContentType {
  NONE,
  JSON,
  FORM,
  FORM_DATA,
  TEXT,
  BLOB,
  ARRAY_BUFFER
}